gulp-bower-jquery-es6-inline functionalities:

ES5:
gulp + bower + jquery

ES6:
gulp + bower + babel/babelify + browserify + jquery

ES6 inline:
gulp + bower + babel/babelify + browserify + jquery + inline
